version: v1.0
name: Deploy
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Deploy
    task:
      jobs:
        - name: Push code
          commands:
            - checkout
            - 'set password [lindex $SERVER_PASSWORD 0]'
            - 'ssh dan@178.62.24.63 '
            - 'expect "password:"'
            - send "$password\r";
            - expect "Are you sure you want to continue connecting (yes/no)?"
            - send "yes"
            - interact
            - sh deploy.sh
